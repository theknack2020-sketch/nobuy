# Closed object set — 12 primitives (M-07), and the coverage manifest
The set is CLOSED at 12. A content item with no row here grows the set before production, never during.

## The set
1 kept tick · 2 spent block · 3 mandatory tick+dot · 4 frozen hollow capsule · 5 not-yet stub · 6 bezel ring (ticks, pitch) · 7 index triangle · 8 rim hand + counterweight · 9 sweep arc · 10 remainder figure (tabular numeral + unit words) · 11 interval bar (settled/open) · 12 well slot row (glyph + label + detail)

Sub-parts: bezel = {ring hairline, majors, minors, printed numerals}; hand = {shaft, counterweight}; cell = {frame, numeral, mark, log dots, note tick, index notch}.

## Coverage (content item × primitives)
| Content item | Primitives used |
|---|---|
| Today's unanswered day | 6,7,8,9,10 |
| The run count / best | 10,9 |
| A kept day (calendar) | 1, cell |
| A spent day + note | 2, cell, 10 |
| Mandatory-only day / logs | 3, cell, dots |
| Frozen day / freeze chip | 4 (chip = 4 filled when used) |
| Unanswered past day | 5, cell + wait ring |
| Pre-record day | bare numeral (deliberate non-object) |
| Urge timer (all states) | 6,7,8,9,10 |
| Waiting-list item + expiry | 12,9(mini),10 |
| Impulse checklist answer | 7-as-seated-tick, 12 |
| Ended runs (stats) | 11,10 |
| Month progress (calendar head) | 9(mini),10 |
| Week kept-rate rail | 10 |
| Paywall standing | 9(two-segment),10 |
| Themes (finishes) | surface swap — no object |
| Widgets (4) | 6,7,9,10 + 1-strip |
| App icon | 6,7,8 (c1) / 7,8 (c2) / 9 (c3) |
| Onboarding illustrations | 6,7,8,9 / cells / device glyph (one-off, listed) |
| Empty states | same objects, empty forms (dashed 11, unlit 9) |

Every row resolves to the set; the set holds at 12. One-off: the onboarding device glyph (screen 3, P6) — declared here, used nowhere else.
